import styled from "styled-components";

const StyledAppHeader = styled.div`
  width: 100%;
`;

export const AppHeader: React.FC = () => {
  return <StyledAppHeader></StyledAppHeader>;
};

export default AppHeader;
